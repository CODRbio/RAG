用户问题："{query}"

以下是搜索结果的摘要片段：
{items_text}

请判断这些摘要是否已包含回答问题所需的具体数据。
如果某条摘要信息残缺（例如有省略号、核心数值/结论被截断），则需要抓取原文。如果现有摘要已足够，无需抓取。
只返回 JSON，格式：{{"urls_to_fetch": ["url1", "url2"]}}。如果全部摘要已足够，返回 {{"urls_to_fetch": []}}。