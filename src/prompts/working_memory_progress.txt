请根据以下最新的项目画布状态，以及之前的历史核心事实，更新并提取当前项目的结构化核心事实 (Facts)。

【提取规则】
1. 将当前项目的核心状态（主题、关键进度、重要洞察、约束条件）提取为简明的 Fact 条目，采用列表形式。
2. 每条 Fact 必须附带更新时间戳（当前时间：{current_time}）。
3. 冲突解决：如果画布状态与“历史事实”发生冲突（例如时间窗口、研究重点发生变化），请用新的画布状态覆盖旧事实，或者将旧事实标记为 [已修订]。对于已不再有效或无用的旧事实可以安全丢弃。

【最新画布状态】
{context}

【历史核心事实】
{old_facts}

请直接输出更新后的结构化 Facts 列表：