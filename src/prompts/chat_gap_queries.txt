Identify concrete information gaps that the current evidence does NOT answer, then produce 1–3 focused search "groups" to fill them. Each group has three query styles for different search engines: recall (broad), precision (focused), discovery (exploratory).

User question:
{query}

Retrieved evidence so far:
{evidence_context}

Instructions:
- A gap is valid only if the evidence does NOT provide a direct answer, mechanism, or data for it. Avoid generic or speculative gaps.
- Each object in "gap_queries" must have: "recall" (broad keywords), "precision" (focused phrase), "discovery" (exploratory or alternative wording). All three should target the same missing piece.
- If the user question is in Chinese, add "precision_zh" (one precision-style phrase in Chinese, 6–12 words) for Google/Scholar.
- Return 1–3 gap objects only. If evidence already suffices, return an empty array.

Return JSON only (no markdown):
{{
  "gap_queries": [
    {{ "recall": "...", "precision": "...", "discovery": "..." }},
    {{ "recall": "...", "precision": "...", "discovery": "...", "precision_zh": "..." }}
  ]
}}
